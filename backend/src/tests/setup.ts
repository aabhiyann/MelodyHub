// Test setup file
// Runs before each test file

// Note: Timeout and mocks configured in jest.config.ts
// ESM doesn't support global jest object in setup files

// Clean up after each test
afterEach(() => {
    // Cleanup logic if needed
});
